"use strict";
"function" === typeof define && define.amd && define([], function() {
  return {
    host: "http://" + location.hostname
  }
});
var webConfig = {
  host: "http://" + location.hostname
};
